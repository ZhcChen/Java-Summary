# L1-M2-S03 线程池核心参数

## 一句话结论

- 线程池核心参数 是 L1 阶段的关键能力点，面试回答建议覆盖“定义、原理、场景、边界”。

## 结构图

```mermaid
flowchart LR
    A[现象识别] --> B[原理分析]
    B --> C[策略设计]
    C --> D[压测验证]
```

## 核心知识点

1. 先区分并发问题类型：竞争、死锁、饥饿、队列积压。
2. 通过线程状态、队列长度、耗时分位数定位瓶颈。
3. 方案必须包含容量保护与回归验证，避免“修一处炸一片”。

## 高频面试题

### Q1：你如何在项目中落地“线程池核心参数”？

答题骨架：
1. 先说明业务目标和约束。
2. 再给可执行方案和关键指标。
3. 最后补充风险、边界与回退策略。

### Q2：线程池核心参数 的常见误区是什么？

答题骨架：
1. 说明常见错误做法。
2. 给出正确实践和适用条件。
3. 用一个真实场景收尾。


## 前置知识

- 理解线程基础概念。
- 知道任务提交和执行的区别。
- 会读构造函数参数。

## 术语解释（零基础友好）

- **核心线程数**：线程池保持存活并优先使用的线程数量。
- **工作队列**：暂存待执行任务的缓冲区。
- **拒绝策略**：线程池无法接收新任务时的处理方式。

## 详细学习步骤（从不会到会）

1. 先用小参数构建线程池，观察任务入队与线程扩容过程。
2. 调整队列大小和最大线程数，比较吞吐与延迟变化。
3. 配置拒绝策略并模拟过载，观察系统行为。
4. 整理一份“参数调整 -> 指标变化”的记录表。

## 常见错误与纠偏

- 直接使用默认工厂，忽略无界队列风险。
- 只关注吞吐，不看队列积压与响应时间。

## 学习动作

- 先手敲一次示例代码，确保可以独立运行。
- 用自己的话复述“定义 -> 原理 -> 场景 -> 边界”。
- 把本节关键结论写成 3 句速记卡，第二天复盘。

## 练习任务（建议动手）

1. 用固定线程池执行 20 个短任务并统计完成时间。
2. 把队列设小并触发拒绝，观察不同策略输出。

## 练习参考方向

- 重点解释为什么会拒绝，以及拒绝后的影响。
- 建议最终参数基于压测结果而非经验值。

## 复习检查

- [ ] 能在 90 秒内说明本节核心结论
- [ ] 能独立运行并解释示例代码输出
- [ ] 能说出至少 1 个常见错误与修正方式

## Java 示例代码（含注释，可直接运行）


**建议文件名：** `Main.java`  
**运行命令：** `javac Main.java && java Main`

**预期输出（示例）：**
```text
result=task-ok
```

```java
import java.util.concurrent.ArrayBlockingQueue;
import java.util.concurrent.Future;
import java.util.concurrent.ThreadPoolExecutor;
import java.util.concurrent.TimeUnit;

public class Main {
    public static void main(String[] args) throws Exception {
        ThreadPoolExecutor pool = new ThreadPoolExecutor(
                1, 2, 30, TimeUnit.SECONDS,
                new ArrayBlockingQueue<>(4),
                new ThreadPoolExecutor.CallerRunsPolicy());

        // 显式指定队列与拒绝策略，避免默认无界风险
        Future<String> f = pool.submit(() -> "task-ok");
        System.out.println("result=" + f.get());
        pool.shutdown();
    }
}
```
