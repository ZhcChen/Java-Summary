# L1-04 高频面试题与答题模板

## 使用方法

- 每题先背“一句话结论”。
- 再按“定义 -> 原理 -> 场景 -> 边界”展开。
- 最后用你项目中的一个真实例子收尾。

## 高频题清单（P0）

| 编号 | 题目 | 核心结论 |
|---|---|---|
| L1-P0-01 | `HashMap` 为什么线程不安全？ | 多线程下存在竞态，需用 `ConcurrentHashMap` |
| L1-P0-02 | `synchronized` 和 `volatile` 区别？ | 前者互斥+可见性，后者只保可见性/有序性 |
| L1-P0-03 | Java 为什么只有值传递？ | 参数始终是值拷贝，引用类型拷贝的是地址值 |
| L1-P0-04 | MySQL 索引为什么快？ | 通过有序结构减少扫描，快速定位数据 |
| L1-P0-05 | Redis 为什么快？ | 内存访问 + 高效数据结构 + 事件驱动模型 |

## 标准答法模板

```text
一句话结论：
核心原理（3点）：
1)
2)
3)
项目场景：
边界和风险：
```

## 示例题（完整答法）

### Q：`HashMap` 为什么线程不安全？

一句话结论：`HashMap` 在并发读写时可能出现数据覆盖、丢失或结构不一致。

核心原理：
1. `put` 过程不是原子操作，多线程会互相覆盖。
2. 扩容迁移期间，多个线程可能看到中间状态。
3. 缺少并发控制机制（锁或分段同步）。

项目场景：
- 本地缓存读多写少时，用 `ConcurrentHashMap` 替代 `HashMap`。

边界和风险：
- `ConcurrentHashMap` 也要关注复合操作原子性，例如“先判断再更新”。

## 训练建议

- 每天 5 题口述，录音回听。
- 回答时控制在 90 秒内，避免失焦。

## Java 示例代码（含注释，可直接运行）

**建议文件名：** `Main.java`  
**运行命令：** `javac Main.java && java Main`

**预期输出（示例）：**
```text
结论先行，3点展开，最后补风险边界
```

```java
public class Main {
    public static void main(String[] args) {
        // 面试表达顺序：结论 -> 原理 -> 场景 -> 边界
        String answer = "结论先行，3点展开，最后补风险边界";
        System.out.println(answer);
    }
}
```
