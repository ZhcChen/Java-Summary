# L1-M2-S05 类加载流程

## 一句话结论

- 类加载流程 是 L1 阶段的关键能力点，面试回答建议覆盖“定义、原理、场景、边界”。

## 结构图

```mermaid
flowchart LR
    A[采集证据] --> B[定位根因]
    B --> C[参数/代码优化]
    C --> D[回归观测]
```

## 核心知识点

1. 先采集 GC 日志、线程栈、堆快照，再下结论。
2. 区分分配速率问题、晋升问题和内存泄漏问题。
3. 参数调整要小步迭代，和业务流量峰值联动验证。

## 高频面试题

### Q1：你如何在项目中落地“类加载流程”？

答题骨架：
1. 先说明业务目标和约束。
2. 再给可执行方案和关键指标。
3. 最后补充风险、边界与回退策略。

### Q2：类加载流程 的常见误区是什么？

答题骨架：
1. 说明常见错误做法。
2. 给出正确实践和适用条件。
3. 用一个真实场景收尾。


## 前置知识

- 知道类和对象关系。
- 会写 `static` 代码块。
- 了解继承基础语法。

## 术语解释（零基础友好）

- **类加载**：把类字节码读入 JVM 并转为运行时结构。
- **初始化**：执行静态变量赋值与静态代码块。
- **双亲委派**：类加载器优先委托父加载器尝试加载。

## 详细学习步骤（从不会到会）

1. 写父子类静态块和实例块，观察实际输出顺序。
2. 区分“加载/链接/初始化”三个阶段职责。
3. 理解双亲委派解决的类冲突与安全问题。
4. 把流程转化为可口述的四步答题模板。

## 常见错误与纠偏

- 误以为 new 对象才会触发所有类加载。
- 混淆静态初始化与实例初始化时机。

## 学习动作

- 先手敲一次示例代码，确保可以独立运行。
- 用自己的话复述“定义 -> 原理 -> 场景 -> 边界”。
- 把本节关键结论写成 3 句速记卡，第二天复盘。

## 练习任务（建议动手）

1. 实现一个父子类并打印初始化顺序。
2. 说出三种会触发类初始化的常见场景。

## 练习参考方向

- 父类静态通常先于子类静态执行。
- 主动使用类（new/访问静态成员等）会触发初始化。

## 复习检查

- [ ] 能在 90 秒内说明本节核心结论
- [ ] 能独立运行并解释示例代码输出
- [ ] 能说出至少 1 个常见错误与修正方式

## Java 示例代码（含注释，可直接运行）


**建议文件名：** `Main.java`  
**运行命令：** `javac Main.java && java Main`

**预期输出（示例）：**
```text
A static
B static
A instance
B instance
```

```java
public class Main {
    static class A {
        static { System.out.println("A static"); }
        { System.out.println("A instance"); }
    }
    static class B extends A {
        static { System.out.println("B static"); }
        { System.out.println("B instance"); }
    }

    public static void main(String[] args) {
        // 首次主动使用时触发类加载与初始化
        new B();
    }
}
```
